import React from "react";
import HomeHeader from "./HomeHeader";

const defExport = {
  title: "Pages/Home/Header",
  component: HomeHeader,
};

export default defExport;
const Template = (args) => <HomeHeader />;
export const Header = Template.bind();
